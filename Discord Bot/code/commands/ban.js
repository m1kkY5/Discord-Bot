const Discord = require('discord.js');

module.exports.run = async (bot, message, args) => {
    console.log('work');
}

module.exports.help = {
    name: "ban",
    description: "Ban users",
    usage: "[@user]"
}